import java.util.Scanner;

public class morningCupCakesTest{
	public static void main(String[] args){
		Scanner input = new Scanner(System.in);
		System.out.print("Enter the number to be checked: ");
		int number = input.nextInt();

		arrayLength = 0
		int[] integers = {2, 6, 9, 6};
		//int result = morningCupCakes.getFirstElement(integers);
		// System.out.print(result);

		/* */
		for (int count = 0; count < integers.length; count++){
			System.out.println(integers[count]);
			if (integers[count] > 0 ){
				//System.out.print(integers[0])
				continue;
			}
		}
		
		

		 /*
		if (integers.length > 0){
			System.out.print(integers[integers.length-1]);			
		} else{	System.out.print("Empty Array");}

		 */
		
		

 		

	}
}